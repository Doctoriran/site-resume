// Language pack (EN / FA)
const i18n = {
  en: {
    kicker: "Portfolio / Musician & Full-Stack Learner",
    h1a: "Mehrad",
    h1b: "Haratian.",
    sub: "Self-taught web developer (HTML/CSS/JS), admin for a 10k+ YouTube channel, and 5-year pianist.",
    marquee: "MEHRAD • Dev • Pianist • Admin • Problem Solver • Security Curious",
    skils: "Skills",
    projects: "Selected Projects",
    music: "Music",
    contact: "Contact",
    cta: "Download Resume",
    see: "see projects",
    skills: "Signature Skills",
    mohsen: "HTML5, CSS3, Modern JS (ES6+), responsive design, accessibility, SEO, performance.",
    admin: "YouTube channel admin (10k+ Member), community moderation, content planning, analytics.",
    ops: "Youtube Ops",
    music3: "Music",
    music2: "5 years of piano. Studio & stage recording, ensemble with teachers, genre: Persian to classical.",
    projectsTitle: "Selected Projects",
    musicTitle: "Music Corner",
    liveTitle: "Live Session",
    liveStatus: "Coming Soon...",
    liveSub: "New performances are being prepared",
    favoritePieces: "Favorite Pieces",
    collabTitle: "Request a Collab",
    collabDesc: "Open to recording sessions, live gigs, and studio collabs.",
    formTitle: "Let's talk",
    formSub: "Business / collab / gigs",
    name: "Your name",
    email: "Email",
    message: "Message",
    send: "Send",
    sent: "Thanks! Your message is flying ✈️",
    footer: "Made with grit, coffee, and late-night code.",
    webStack: "Web Stack",
    adminOps: "Admin & Ops",
    securityCurious: "Security Curious",
    leadership: "Leadership",
    languages: "Languages",
    webStackDesc: "HTML5, CSS3, Modern JS (ES6+), responsive design, accessibility, SEO, performance.",
    adminOpsDesc: "YouTube channel admin (10k+ Member), community moderation, content planning, analytics.",
    securityCuriousDesc: "Basics of web security, testing & hardening forms, XSS/CSRF awareness, safe deploys.",
    leadershipDesc: "Team coordination in classes/groups, delivering results under pressure with calm focus.",
    languagesDesc: "Persian (native), English (upper‑intermediate). Admin-level communication & writing.",
    project1Title: "Channel Admin Suite",
    project1Desc: "Tools for message management, scheduling, and auto‑responses (YouTube + Telegram).",
    project2Title: "Mehrad.dev",
    project2Desc: "This portfolio as a PWA: offline ready, 3D background, bilingual (EN/FA), dark/light.",
    project3Title: "Music Collab",
    project3Desc: "Recorded performances with teachers and ensembles; 5+ published videos.",
    piece1: "Persian classics",
    piece2: "Film scores",
    piece3: "Modern ballads",
    shineTitle: "Where I shine",
    shineDesc: "Front-end craft, community ops, and music storytelling. Available for remote gigs.",
    themeBtn: "Theme"
  },
  fa: {
    kicker: "رزومه / موزیسین و توسعه‌دهنده وب",
    h1a: "مهراد",
    h1b: "هراتیان.",
    sub: "توسعه‌دهنده خودآموخته وب (HTML/CSS/JS)، ادمین یک کانال یوتیوب ۱۰ هزار نفره و پیانیست ۵ ساله.",
    marquee: "مهراد • برنامه‌نویس • پیانیست • ادمین • حل‌کننده مشکل • علاقه‌مند به امنیت",
    skils: "مهارت‌ها",
    projects: "پروژه‌های منتخب",
    music: "موسیقی",
    contact: "تماس",
    cta: "دانلود رزومه",
    see: "مشاهده پروژه‌ها",
    skills: "مهارت‌های شاخص",
    mohsen: "HTML5، CSS3، جاوااسکریپت مدرن (ES6+)، طراحی واکنش‌گرا، دسترس‌پذیری، سئو، بهینه‌سازی عملکرد.",
    admin: "ادمین کانال یوتیوب (با بیش از ۱۰ هزار عضو)، مدیریت انجمن، برنامه‌ریزی محتوا، تحلیل داده.",
    ops: "امور یوتیوب",
    music3: "موسیقی",
    music2: "۵ سال تجربه پیانو. ضبط استودیویی و صحنه‌ای، هم‌نوازی با اساتید، سبک: ایرانی تا کلاسیک.",
    projectsTitle: "پروژه‌های منتخب",
    musicTitle: "گوشه موسیقی",
    liveTitle: "اجرای زنده",
    liveStatus: "به زودی...",
    liveSub: "اجراهای جدید در حال آماده‌سازی",
    favoritePieces: "قطعات مورد علاقه",
    collabTitle: "درخواست همکاری",
    collabDesc: "برای جلسات ضبط، اجرای زنده و همکاری‌های استودیویی آماده‌ام.",
    formTitle: "در تماس باشیم",
    formSub: "کسب‌وکار / همکاری / اجرا",
    name: "نام شما",
    email: "ایمیل",
    message: "پیام",
    send: "ارسال",
    sent: "مرسی! پیام شما پرواز کرد ✈️",
    footer: "ساخته شده با تلاش، قهوه و کدهای نیمه‌شب.",
    webStack: "توسعه وب",
    adminOps: "مدیریت و عملیات",
    securityCurious: "علاقه‌مند به امنیت",
    leadership: "رهبری",
    languages: "زبان‌ها",
    webStackDesc: "HTML5، CSS3، جاوااسکریپت مدرن (ES6+)، طراحی واکنش‌گرا، دسترس‌پذیری، سئو، بهینه‌سازی عملکرد.",
    adminOpsDesc: "ادمین کانال یوتیوب (با بیش از ۱۰ هزار عضو)، مدیریت انجمن، برنامه‌ریزی محتوا، تحلیل داده.",
    securityCuriousDesc: "مبانی امنیت وب، تست و امن‌سازی فرم‌ها، آگاهی از XSS/CSRF، استقرار امن.",
    leadershipDesc: "هماهنگی تیم در کلاس‌ها و گروه‌ها، ارائه نتایج تحت فشار با تمرکز و آرامش.",
    languagesDesc: "فارسی (زبان مادری)، انگلیسی (بالای متوسط). ارتباطات و نوشتار در سطح مدیریت.",
    project1Title: "ابزار مدیریت کانال",
    project1Desc: "ابزارهای مدیریت پیام، زمان‌بندی و پاسخ‌گوی خودکار (یوتیوب + تلگرام).",
    project2Title: "مهراد.دو",
    project2Desc: "این نمونه‌کار به صورت PWA: آفلاین، پس‌زمینه سه‌بعدی، دو زبانه (EN/FA)، دارک/لایت.",
    project3Title: "همکاری موسیقی",
    project3Desc: "اجراهای ضبط شده با اساتید و گروه‌ها؛ بیش از ۵ ویدیو منتشر شده.",
    piece1: "کلاسیک‌های ایرانی",
    piece2: "موسیقی متن فیلم",
    piece3: "بالادهای مدرن",
    shineTitle: "نقطه قوت من",
    shineDesc: "توسعه فرانت‌اند، عملیات انجمن و داستان‌سرایی موسیقی. برای همکاری‌های راه‌دور در دسترس‌ام.",
    themeBtn: "تم"
  }
};

let lang = localStorage.getItem("lang") || "en";
const t = (k) => i18n[lang][k];

function applyLang() {
  document.querySelectorAll("[data-i]").forEach(el => {
    const key = el.getAttribute("data-i");
    if (i18n[lang][key]) {
      el.textContent = i18n[lang][key];
    }
  });
  document.dir = (lang === "fa" ? "rtl" : "ltr");
}

document.addEventListener("click", (e) => {
  const trg = e.target.closest("[data-lang]");
  if (trg) {
    lang = trg.getAttribute("data-lang");
    localStorage.setItem("lang", lang);
    applyLang();
  }
});

const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
let theme = localStorage.getItem("theme") || (prefersDark ? "dark" : "dark");

function setTheme(th) {
  document.documentElement.style.setProperty('--bg', th === "dark" ? '#0b0b0f' : '#f7f7fb');
  document.documentElement.style.setProperty('--bg-soft', th === "dark" ? '#101018' : '#ffffff');
  document.documentElement.style.setProperty('--card', th === "dark" ? '#121222' : '#ffffff');
  document.documentElement.style.setProperty('--text', th === "dark" ? '#e6e6f0' : '#11121a');
  document.documentElement.style.setProperty('--muted', th === "dark" ? '#9aa0aa' : '#5a5f6b');
  localStorage.setItem("theme", th);
}

document.addEventListener("click", e => {
  if (e.target.closest("[data-theme]")) {
    theme = theme === "dark" ? "light" : "dark";
    setTheme(theme);
  }
});

let renderer, scene, camera, points;

function init3D() {
  const canvas = document.getElementById("bg");
  if (!canvas) return;
  renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
  renderer.setSize(window.innerWidth, window.innerHeight);
  scene = new THREE.Scene();
  camera = new THREE.PerspectiveCamera(60, window.innerWidth / window.innerHeight, .1, 1000);
  camera.position.z = 5;

  const geometry = new THREE.BufferGeometry();
  const N = 4000;
  const positions = new Float32Array(N * 3);
  for (let i = 0; i < N; i++) {
    positions[i * 3 + 0] = (Math.random() - 0.5) * 8;
    positions[i * 3 + 1] = (Math.random() - 0.5) * 8;
    positions[i * 3 + 2] = (Math.random() - 0.5) * 8;
  }
  geometry.setAttribute("position", new THREE.BufferAttribute(positions, 3));
  const material = new THREE.PointsMaterial({ size: 0.02, color: 0xffffff });
  points = new THREE.Points(geometry, material);
  scene.add(points);

  window.addEventListener("resize", () => {
    renderer.setSize(window.innerWidth, window.innerHeight);
    camera.aspect = window.innerWidth / window.innerHeight;
    camera.updateProjectionMatrix();
  });

  animate();
}

function animate() {
  requestAnimationFrame(animate);
  if (points) {
    points.rotation.y += 0.0008;
    points.rotation.x += 0.0004;
  }
  if (renderer && scene && camera) {
    renderer.render(scene, camera);
  }
}

function setupCarousel() {
  const track = document.querySelector(".track");
  if (!track) return;
  let isDown = false, startX, scrollLeft;
  track.addEventListener("mousedown", (e) => { isDown = true; startX = e.pageX - track.offsetLeft; scrollLeft = track.scrollLeft; });
  track.addEventListener("mouseleave", () => isDown = false);
  track.addEventListener("mouseup", () => isDown = false);
  track.addEventListener("mousemove", (e) => {
    if (!isDown) return;
    e.preventDefault();
    const x = e.pageX - track.offsetLeft;
    const walk = (x - startX) * 2;
    track.scrollLeft = scrollLeft - walk;
  });
}

function setupForm() {
  const form = document.querySelector("form#contact");
  if (!form) return;
  const out = document.getElementById("sent");
  form.addEventListener("submit", (e) => {
    e.preventDefault();
    const data = new FormData(form);
    const name = data.get("name");
    const email = data.get("email");
    const message = data.get("message");
    const subject = encodeURIComponent("New message from portfolio");
    const body = encodeURIComponent(`Name: ${name}\nEmail: ${email}\n\n${message}`);
    window.location.href = `mailto:contact@mehrad.dev?subject=${subject}&body=${body}`;
    if (out) out.textContent = t("sent");
    form.reset();
  });
}

if ("serviceWorker" in navigator) {
  window.addEventListener("load", () => navigator.serviceWorker.register("./service-worker.js"));
}

window.addEventListener("DOMContentLoaded", () => {
  setTheme(theme);
  applyLang();
  setupCarousel();
  setupForm();
  init3D();
});

(function typing() {
  const el = document.getElementById("type");
  if (!el) return;
  const words = ["Developer", "Pianist", "Admin", "Dreamer"];
  let i = 0, j = 0, dir = 1;

  function tick() {
    el.textContent = words[i].slice(0, j);
    j += dir;
    if (j > words[i].length + 6) { dir = -1; }
    if (j < 0) { dir = 1; i = (i + 1) % words.length; }
    setTimeout(tick, 90);
  }
  tick();
})();

const obs = new IntersectionObserver((entries) => {
  for (const e of entries) {
    if (e.isIntersecting) {
      e.target.style.transform = "translateY(0) scale(1)";
      e.target.style.opacity = 1;
    } else {
      e.target.style.opacity = 0;
    }
  }
}, { threshold: .2 });

document.addEventListener("DOMContentLoaded", () => {
  document.querySelectorAll(".reveal").forEach(el => {
    el.style.transform = "translateY(18px) scale(.98)";
    el.style.opacity = 0;
    el.style.transition = "all .7s ease";
    obs.observe(el);
  });
});
